description: 'AWS WAF is a web application firewall that lets you monitor the HTTP(S)
  requests that are forwarded to your protected web application resources.

  Sending AWS WAF events to Sekoia.io will help you to analyze in real time activities
  from AWS accounts and detect unusual or malicious behaviors.'
name: aws_waf
produces:
  events:
  - mode: pull
vendor: AWS
