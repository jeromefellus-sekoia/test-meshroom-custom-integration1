description: 'NetFlow is a protocol offering the ability to watch IP flow information
  from data network

  This format aims to be used with Sekoia.io Netflow collector.'
name: netflow_collector
produces:
  events:
  - {}
vendor: NetFlow
