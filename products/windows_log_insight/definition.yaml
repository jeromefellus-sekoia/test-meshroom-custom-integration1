description: 'Log Insight Windows Agent is a solution to collect Microsoft Windows
  logs.

  Logs generated by the different Windows services are very valuable to detect any
  kind of threat, thanks to numerous behavior detection rules written by the SEKOIA
  analysts. Sending Windows logs allows to detect lateralization and malicious user
  activities, and brings you visibility on the attack path and the compromised workstations.'
name: windows_log_insight
produces:
  events:
  - {}
vendor: Log Insight Windows
